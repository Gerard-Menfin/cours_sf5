{# COMMENTAIRES #}
{# {{ }}  les doubles accolades remplacent la balise PHP echo <?= ?> 

{% balise %}  : les balises twig sont souvent fermées par {% endbalise %}

Le symbole de la concaténation : ~
Tous les opérateurs arithmétiques sont utilisables dans twig : +-*/
Tous les opérateurs de comparaison  sont utilisables dans twig : > < >= <=
Tous les opérateurs d'égalité : == != 


#}

<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {{ include("debug.html.twig") }}
        <div class="container">
            {% block nav %} 
                {{ include("nav.html.twig") }}
            {% endblock %}

            {# Pour afficher les messages flash, on utilise la variable app.
                app.flashes est un array associatif dont les indices sont les types de 
                messages. Chaque valeur est un array (ce qui veut dire qu'il peut y
                avoir plusieurs messages de type "success" en session en même temps).
                Une fois que les messages seront affichés, ils disparaitront de la session.
            #}
            {% for type, messages in app.flashes %}
                <div class="alert alert-{{ type }}">
                    {% for msg in messages %}
                        {{ msg|raw }}
                    {% endfor %}
                </div>
            {% endfor %}

            <h1>{% block h1 %}Bienvenue à la bibliothèque{% endblock %}</h1>

            {% block body %}
                <p>Ceci est le contenu de la balise twig <code>block body</code> du fichier
                    <i>base.html.twig</i>
                </p>
            {% endblock %}

            <footer class="bg-dark text-light mt-5">
                {% block piedDePage %}
                    ceci est le footer
                {% endblock %}
            </footer>
        </div>
    </body>
</html>
